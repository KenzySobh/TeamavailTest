# TeamAvail — Local CI/CD (minimal)

## Requirements
- Docker & Docker Compose
- Python 3.11 (for dev tools)
- pip

## Quick run (local)
1. Install dev tools (if needed):
   ```bash
   pip install -r requirements-dev.txt
